name: onebot_flutter
description: A Dart/Flutter SDK for the OneBot v11 protocol. Connect to NoneBot, NapCatQQ, LLOneBot, and other OneBot-compatible implementations.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
